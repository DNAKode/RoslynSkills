You are continuing work in `DNAKode/RoslynSkills` (DNA Kode), an engineering-first research program evaluating whether Roslyn-native tooling materially improves agentic coding for C#/.NET versus text-first workflows. Treat production quality and empirical evidence as first-class goals: implement robust capabilities, validate them with hard gates, and avoid claims that are not backed by reproducible artifacts.

At session start, enumerate the Roslyn command surface with `dotnet run --project src/RoslynSkills.Cli -- list-commands --ids-only`, then default to Roslyn commands for navigation, context, diagnostics, edits, and repair. Prefer shorthand `scripts/roscli(.cmd)` calls for simple operations and use JSON stdin only for structured payloads. If you fall back to non-Roslyn `.cs` reads/edits, add an explicit entry to `ROSLYN_FALLBACK_REFLECTION_LOG.md` with reason, missing command hypothesis, and expected impact.

Current repo status: root README is present and aligned with project intent/status; the CLI exposes 32 commands including `ctx.file_outline`, `ctx.member_source`, `edit.transaction`, and `session.*`; paired-run harness reliability includes publish-once CLI usage, control contamination gating, deterministic constraint checks, per-run environment isolation, and summary markdown/token attribution outputs; core, CLI, and benchmark tests pass on `dotnet test RoslynSkills.slnx -c Release`.

Immediate priority is to run and analyze real paired baseline-vs-Roslyn sessions on meaningful multi-step tasks (utility/game plus follow-up direction changes or higher-ambiguity OSS tasks), with Codex and Claude where available. Capture full transcripts, run metadata, token totals (including cache-inclusive views), retries, Roslyn attempted/successful calls, correctness outcomes, and notable failure/recovery fragments. Use those artifacts to update benchmark reports without over-claiming and to identify the next high-leverage Roslyn command-surface improvements.

Operate with high autonomy and tight iteration loops: inspect with Roslyn, apply targeted edits, validate quickly, and keep docs synchronized with actual behavior. Keep AGENTS meta-learning concise and dependency-driven, keep benchmark workflow docs aligned with current scripts/outputs, and end each session with a clean git state, clear validation evidence, and a short handoff note stating what changed, what passed, what is blocked, and what should run next.

